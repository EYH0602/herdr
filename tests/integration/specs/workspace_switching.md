# Test: Workspace switching with prefix+N

## Steps

1. Press `n` to create a workspace named `first`
2. Press prefix (ctrl+s), then `n` to create a second workspace named `second`
3. Read the screen and verify the sidebar shows two workspaces numbered 1 and 2
4. Verify `second` is the active workspace (has the `▸` marker)
5. Press prefix (ctrl+s), then `1` to switch to the first workspace
6. Read the screen and verify `first` is now active (has the `▸` marker)
7. Press prefix (ctrl+s), then `2` to switch back to `second`
8. Verify `second` is active again
