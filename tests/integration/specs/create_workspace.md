# Test: Create workspace

## Steps

1. Read the screen to verify herdr started in empty state (should show "No active workspace" or "Press n to create one")
2. Press `n` to enter create mode
3. Type `test-workspace`
4. Press Enter to create
5. Read the screen and verify:
   - The sidebar shows `1` followed by `test-workspace`
   - A shell prompt is visible in the main pane area
   - The active marker `▸` is next to the workspace
